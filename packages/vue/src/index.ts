import { defineComponent, h, onBeforeUnmount, onMounted, ref, watch, type PropType } from 'vue';
import { createEditorUI } from '@sheditor/ui';
import type { Locale, SHEditorAPI as Editor } from '@sheditor/core';
export const SHEditor = defineComponent({ name:'SHEditor', props:{ modelValue:{type:String,default:''},locale:{type:String as PropType<Locale>,default:'en'},editable:{type:Boolean,default:true},placeholder:{type:String,default:''} }, emits:['update:modelValue','ready'], setup(props,{emit,expose}){ const host=ref<HTMLElement>(); let editor:Editor|null=null; let destroy:()=>void=()=>{}; onMounted(()=>{const ui=createEditorUI(host.value!,{content:props.modelValue,locale:props.locale,editable:props.editable,placeholder:props.placeholder,onUpdate:({editor:e})=>emit('update:modelValue',e.getHTML())});editor=ui.editor;destroy=ui.destroy;emit('ready',editor)}); watch(()=>props.modelValue,value=>{if(editor&&editor.getHTML()!==value)editor.setContent(value,false)}); watch(()=>props.editable,value=>editor?.setEditable(value)); onBeforeUnmount(()=>destroy()); expose({getEditor:()=>editor,focus:()=>editor?.focus()}); return()=>h('div',{ref:host}); } });
export type { Editor };
